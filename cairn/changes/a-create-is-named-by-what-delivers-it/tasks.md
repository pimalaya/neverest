---
cairn: tasks
change: a-create-is-named-by-what-delivers-it
---

- [x] `ItemHunk::Add`, `ItemHunk::Copy::origin`, their display.
- [x] `placement_hunks` reads the placement's origin and whether a side syncs alone; `names` matches an add.
- [x] Unit test over the three deliveries; a dry run over a queued card reads `add item … on carddav`.
- [x] sync.md folded; the log entry written.
